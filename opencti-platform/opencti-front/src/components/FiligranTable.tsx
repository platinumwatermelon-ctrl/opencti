import React from 'react';
import Table, { TableProps } from '@mui/material/Table';

type FiligranTableProps = TableProps;

const FiligranTable = (props: FiligranTableProps) => <Table {...props} />;

export default FiligranTable;
