import React from 'react';
import AlertTitle, { AlertTitleProps } from '@mui/material/AlertTitle';

type FiligranAlertTitleProps = AlertTitleProps;

const FiligranAlertTitle = (props: FiligranAlertTitleProps) => <AlertTitle {...props} />;

export default FiligranAlertTitle;
