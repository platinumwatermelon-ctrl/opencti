import React from 'react';
import Tooltip, { TooltipProps } from '@mui/material/Tooltip';

type FiligranTooltipProps = TooltipProps;

const FiligranTooltip = (props: FiligranTooltipProps) => <Tooltip {...props} />;

export default FiligranTooltip;
