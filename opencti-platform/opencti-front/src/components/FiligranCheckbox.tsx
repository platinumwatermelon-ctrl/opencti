import React from 'react';
import Checkbox, { CheckboxProps } from '@mui/material/Checkbox';

type FiligranCheckboxProps = CheckboxProps;

const FiligranCheckbox = (props: FiligranCheckboxProps) => <Checkbox {...props} />;

export default FiligranCheckbox;
