import React from 'react';
import Tab, { TabProps } from '@mui/material/Tab';

type FiligranTabProps = TabProps;

const FiligranTab = (props: FiligranTabProps) => <Tab {...props} />;

export default FiligranTab;
