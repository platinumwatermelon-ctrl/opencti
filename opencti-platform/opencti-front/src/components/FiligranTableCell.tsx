import React from 'react';
import TableCell, { TableCellProps } from '@mui/material/TableCell';

type FiligranTableCellProps = TableCellProps;

const FiligranTableCell = (props: FiligranTableCellProps) => <TableCell {...props} />;

export default FiligranTableCell;
