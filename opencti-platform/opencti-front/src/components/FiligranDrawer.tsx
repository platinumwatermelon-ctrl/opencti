import React from 'react';
import Drawer, { DrawerProps } from '@mui/material/Drawer';

type FiligranDrawerProps = DrawerProps;

const FiligranDrawer = (props: FiligranDrawerProps) => <Drawer {...props} />;

export default FiligranDrawer;
