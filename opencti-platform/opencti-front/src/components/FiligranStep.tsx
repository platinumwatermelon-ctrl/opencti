import React from 'react';
import Step, { StepProps } from '@mui/material/Step';

type FiligranStepProps = StepProps;

const FiligranStep = (props: FiligranStepProps) => <Step {...props} />;

export default FiligranStep;
