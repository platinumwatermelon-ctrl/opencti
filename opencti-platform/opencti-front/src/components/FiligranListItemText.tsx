import React from 'react';
import ListItemText, { ListItemTextProps } from '@mui/material/ListItemText';

type FiligranListItemTextProps = ListItemTextProps;

const FiligranListItemText = (props: FiligranListItemTextProps) => <ListItemText {...props} />;

export default FiligranListItemText;
