import React from 'react';
import Toolbar, { ToolbarProps } from '@mui/material/Toolbar';

type FiligranToolbarProps = ToolbarProps;

const FiligranToolbar = (props: FiligranToolbarProps) => <Toolbar {...props} />;

export default FiligranToolbar;
