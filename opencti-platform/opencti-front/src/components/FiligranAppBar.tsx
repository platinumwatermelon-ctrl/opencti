import React from 'react';
import AppBar, { AppBarProps } from '@mui/material/AppBar';

type FiligranAppBarProps = AppBarProps;

const FiligranAppBar = (props: FiligranAppBarProps) => <AppBar {...props} />;

export default FiligranAppBar;
