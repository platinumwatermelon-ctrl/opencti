import React from 'react';
import Popover, { PopoverProps } from '@mui/material/Popover';

type FiligranPopoverProps = PopoverProps;

const FiligranPopover = (props: FiligranPopoverProps) => <Popover {...props} />;

export default FiligranPopover;
