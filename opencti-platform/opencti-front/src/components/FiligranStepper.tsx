import React from 'react';
import Stepper, { StepperProps } from '@mui/material/Stepper';

type FiligranStepperProps = StepperProps;

const FiligranStepper = (props: FiligranStepperProps) => <Stepper {...props} />;

export default FiligranStepper;
