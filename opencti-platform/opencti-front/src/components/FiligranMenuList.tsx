import React from 'react';
import MenuList, { MenuListProps } from '@mui/material/MenuList';

type FiligranMenuListProps = MenuListProps;

const FiligranMenuList = (props: FiligranMenuListProps) => <MenuList {...props} />;

export default FiligranMenuList;
