import React from 'react';
import Collapse, { CollapseProps } from '@mui/material/Collapse';

type FiligranCollapseProps = CollapseProps;

const FiligranCollapse = (props: FiligranCollapseProps) => <Collapse {...props} />;

export default FiligranCollapse;
