import React from 'react';
import Slider, { SliderProps } from '@mui/material/Slider';

type FiligranSliderProps = SliderProps;

const FiligranSlider = (props: FiligranSliderProps) => <Slider {...props} />;

export default FiligranSlider;
