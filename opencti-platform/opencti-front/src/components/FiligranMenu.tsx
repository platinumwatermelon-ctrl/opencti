import React from 'react';
import Menu, { MenuProps } from '@mui/material/Menu';

type FiligranMenuProps = MenuProps;

const FiligranMenu = (props: FiligranMenuProps) => <Menu {...props} />;

export default FiligranMenu;
