import React from 'react';
import Dialog, { DialogProps } from '@mui/material/Dialog';

type FiligranDialogProps = DialogProps;

const FiligranDialog = (props: FiligranDialogProps) => <Dialog {...props} />;

export default FiligranDialog;
