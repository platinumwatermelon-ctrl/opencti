import React from 'react';
import ListItemIcon, { ListItemIconProps } from '@mui/material/ListItemIcon';

type FiligranListItemIconProps = ListItemIconProps;

const FiligranListItemIcon = (props: FiligranListItemIconProps) => <ListItemIcon {...props} />;

export default FiligranListItemIcon;
