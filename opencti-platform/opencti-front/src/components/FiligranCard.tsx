import React from 'react';
import Card, { CardProps } from '@mui/material/Card';

type FiligranCardProps = CardProps;

const FiligranCard = (props: FiligranCardProps) => <Card {...props} />;

export default FiligranCard;
