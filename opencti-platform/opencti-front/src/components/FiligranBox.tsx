import React from 'react';
import { Box, BoxProps } from '@mui/material';

type FiligranBoxProps = BoxProps;

const FiligranBox = (props: FiligranBoxProps) => <Box {...props} />;

export default FiligranBox;
