import React from 'react';
import Tabs, { TabsProps } from '@mui/material/Tabs';

type FiligranTabsProps = TabsProps;

const FiligranTabs = (props: FiligranTabsProps) => <Tabs {...props} />;

export default FiligranTabs;
