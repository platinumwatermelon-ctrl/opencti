import React from 'react';
import Modal, { ModalProps } from '@mui/material/Modal';

type FiligranModalProps = ModalProps;

const FiligranModal = (props: FiligranModalProps) => <Modal {...props} />;

export default FiligranModal;
