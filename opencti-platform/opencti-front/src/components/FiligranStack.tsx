import React from 'react';
import Stack, { StackProps } from '@mui/material/Stack';

type FiligranStackProps = StackProps;

const FiligranStack = (props: FiligranStackProps) => <Stack {...props} />;

export default FiligranStack;
