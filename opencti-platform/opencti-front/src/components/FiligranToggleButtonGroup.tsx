import React from 'react';
import ToggleButtonGroup, { ToggleButtonGroupProps } from '@mui/material/ToggleButtonGroup';

type FiligranToggleButtonGroupProps = ToggleButtonGroupProps;

const FiligranToggleButtonGroup = (props: FiligranToggleButtonGroupProps) => <ToggleButtonGroup {...props} />;

export default FiligranToggleButtonGroup;
