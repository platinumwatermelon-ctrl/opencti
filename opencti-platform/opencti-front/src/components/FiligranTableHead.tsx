import React from 'react';
import TableHead, { TableHeadProps } from '@mui/material/TableHead';

type FiligranTableHeadProps = TableHeadProps;

const FiligranTableHead = (props: FiligranTableHeadProps) => <TableHead {...props} />;

export default FiligranTableHead;
