import React from 'react';
import Alert, { AlertProps } from '@mui/material/Alert';

type FiligranAlertProps = AlertProps;

const FiligranAlert = (props: FiligranAlertProps) => <Alert {...props} />;

export default FiligranAlert;
